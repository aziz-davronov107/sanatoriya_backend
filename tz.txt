User/Profile

Jadval: User
API:
GET /profile/me — Profilni olish
PUT /profile/me — Profilni tahrirlash
POST /auth/register — Ro‘yxatdan o‘tish
POST /auth/login — Login
POST /auth/refresh — Token yangilash
Category

Jadval: Category
API:
GET /category — Barcha kategoriyalar
GET /category/:id — Bitta kategoriya
POST /category — Kategoriya yaratish
PUT /category/:id — Kategoriya tahrirlash
DELETE /category/:id — Kategoriya o‘chirish
Accommodation (E’lonlar)

Jadval: Accommodation
API:
GET /accommodation — Barcha e’lonlar
GET /accommodation/:id — Bitta e’lon
POST /accommodation — E’lon yaratish
PUT /accommodation/:id — E’lon tahrirlash
DELETE /accommodation/:id — E’lon o‘chirish
GET /accommodation/my — Mening e’lonlarim
Booking

Jadval: Booking
API:
GET /booking — Barcha bookinglar
GET /booking/:id — Bitta booking
POST /booking — Booking yaratish
PUT /booking/:id — Booking tahrirlash
DELETE /booking/:id — Booking o‘chirish
Contact

Jadval: Contact
API:
POST /contact — Xabar yuborish (feedback, savol va h.k.)
Like

Jadval: Like
API:
POST /like — Like qo‘yish
DELETE /like — Like olib tashlash
Rating

Jadval: Rating
API:
POST /rating — Reyting qo‘yish
GET /rating/:accommodationId — E’lon reytingini olish
Verification (OTP)

Jadval: yo‘q (Redis orqali)
API:
POST /verification/send-otp — OTP yuborish
POST /verification/verify-otp — OTP tekshirish